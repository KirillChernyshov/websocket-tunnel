# Server Configuration (for work server)
PORT=3000
WS_PORT=3001

# Client Configuration (for local PC)
SERVER_WS_URL=ws://your-work-server.com:3001
LOCAL_TARGET=http://localhost:8080
CLIENT_ID=my-pc-client
CLIENT_NAME=My Local PC

# Optional: Custom timeouts (in milliseconds)
REQUEST_TIMEOUT=30000
HEARTBEAT_INTERVAL=30000
RECONNECT_INTERVAL=5000
